---
"@usehercules/convex": minor
---

Separate deployment entry, end-user IAM management, and trusted service actions
into explicit package surfaces. Require typed user or principal recipients for
management writes and report runtime checks against grouping permission keys.
