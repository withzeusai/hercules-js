---
"@usehercules/convex": major
---

Separate deployment entry, end-user IAM management, and trusted service actions
into explicit package surfaces. Require typed user or principal recipients for
management writes, report runtime checks against grouping permission keys, and
expose structured runtime IAM error classification for application access UX.
