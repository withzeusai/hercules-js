---
"@usehercules/auth": patch
---

Show an actionable error instead of a blank app when browser security settings deny storage during auth initialization. Preserve configured OIDC storage and offer an embedded app a clean new-tab entry point without forwarding authentication credentials.
